---
usage_report:
  format: string
  settable: false
engine_report:
  format: string
  settable: false
device_info:
  format: string
  settable: false
engine_fetch:
  format: string
fetch_status:
  format: string
update_interval:
  format: string
sensors_report:
  format: string

